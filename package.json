{
  "name": "async-memo-ize",
  "version": "0.0.5",
  "description": "Simple async memoize utility with cache in memory or via Redis",
  "main": "build/index.js",
  "repository": "git@github.com:aboutlo/async-memoize.git",
  "author": "Lorenzo Sicilia <lorenzo.sicilia@gmail.com>",
  "engines": {
    "node": ">=6.0.0"
  },
  "keywords": [
    "memoize",
    "memoization",
    "async",
    "cache",
    "u"
  ],
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-plugin-add-module-exports": "^0.2.1",
    "babel-preset-env": "^1.6.0",
    "chai": "^4.1.0",
    "mocha": "^3.4.2",
    "prettier": "^1.5.3",
    "sinon": "^2.3.8",
    "sinon-chai": "^2.11.0"
  },
  "scripts": {
    "build": "babel -d build lib/",
    "publish": "npm publish",
    "start": "node -r babel-register lib/",
    "test": "mocha --require ./test/bootstrap.js test/ --recursive --compilers js:babel-register",
    "watch": "mocha --require ./test/bootstrap.js test/ --recursive -w --compilers js:babel-register",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "bluebird": "^3.5.0"
  },
  "peerDependencies": {
    "redis": "^2.7.1"
  }
}
